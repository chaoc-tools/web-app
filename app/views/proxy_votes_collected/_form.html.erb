<%= form_for(@proxy_votes_collected) do |f| %>
  <% if @proxy_votes_collected.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@proxy_votes_collected.errors.count, "error") %> prohibited this proxy_votes_collected from being saved:</h2>

      <ul>
      <% @proxy_votes_collected.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :annual_meeting_id %><br>
    <%= f.number_field :annual_meeting_id %>
  </div>
  <div class="field">
    <%= f.label :building_id %><br>
    <%= f.number_field :building_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :votes_collected %><br>
    <%= f.number_field :votes_collected %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
